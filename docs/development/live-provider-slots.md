# Exclusive live-provider trial slots

Coordinator: Session A, codex/app-integration. No active slot granted at foundation publication. No paid calls have been made by Session A. Credentials remain in private .env; do not put values here.

| Owner | Requested first allocation | State | Transfer requirement |
| --- | --- | --- | --- |
| Word by Word | 1 FastH3 session, <=180s provider lifetime, 120s app / 30s step | Waiting for operator account/session readiness | attempt count + four saved clips/failed gate + independently confirmed terminal provider state |
| Prompt Royale | 1 Helios session, <=60s lifetime, >=6s source / 5s output | Waiting for operator account/session readiness | attempt count + saved playable source + terminal provider state |
| Reverse Prompt | 9-attempt persistent campaign requested; first independent capture 1 attempt | Waiting for explicit campaign allocation and operator account/session readiness | persistent ledger preserved + terminal provider state; uncertainty blocks every worktree |

SDK 1.5.1 native import verified by all owners on Darwin arm64/Python3.13. SDK disconnect can swallow termination errors: disconnect alone is not proof; verify coordinator session terminal state/404. No transfer until closure confirmed. Fixture work can proceed with explicit failed live gate if external readiness blocks the spike. Game owners report attempts, closure, evidence, dependency changes and committed SHAs in their handoffs.

FFmpeg/ffprobe 7.1.1 installed and verified by A. Operator readiness question pending; slots remain ungranted.

Read-only readiness attempt, 12 September: the official billing documentation says account usage/billing APIs are still in development; the dashboard redirects to sign-in in the available browser. No account-wide session-list endpoint was established. Operator balance and prior-session closure confirmation remain the exact blockers. No paid slot or persistent campaign allocation was issued.
