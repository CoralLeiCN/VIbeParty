# Word by Word live app test — 12 September 2026

**Overall: fail.** The real FastH3 run generated its first clip, but capture received 157 of the advertised 158 frames. The first step timed out after 30.009 seconds. No playable clip was saved, so the scene additions, continuity, reveal, and replay could not be verified. Independent provider lookup confirmed `CLOSED`.

## Scenario and setup

- Tester: Codex, through the app's host and player browser forms.
- Fixture version: `categories-v1`; scenario: `WW-CAT-01`, repeat 1 in this task.
- Flow: one-player demo; separate display host and player named `Scenario Tester`, assigned all four slots.
- Mode/model: real live `reactor/fast-h3`, reactor-sdk 1.5.1, Python 3.13.3, Darwin arm64.
- Rendering: six-second request per clip, 16:9, 24 fps local H.264 capture; existing illustrated fixed-camera prompt. No starting image or seed supplied.
- Local app: this worktree's built frontend and backend on port 8011, separate private media directory. The existing server on port 8000 was left running.
- One live session attempt; no additional generation attempt or reroll.

Exact answers, with the theme mapped to Place and Scene change mapped to Consequence:

| Slot | Accepted answer |
| --- | --- |
| Place | Enchanted forest |
| Character | A fox wearing a crown |
| Action | Dances ballet |
| Consequence | Glowing snow begins falling |

## Browser observations

| Check | Result |
| --- | --- |
| One-player assignments | Pass: all four categories appeared on the player screen. |
| Instructions and examples | Partial: category questions, the one-idea instruction, editable empty inputs, and 120-character counters appeared. Example hints still used `a moonlit forest`, `a fox in a tiny hat`, `They start breakdancing.`, and `Confetti rains from the sky.`, rather than the standard scenario answers. |
| Independent submission order | Pass: submitted Consequence → Character → Place → Action within the 45-second collection window. |
| Locking | Pass for the observed accepted fields: each became read-only accepted text with “Accepted and locked.” before generation began. |
| Public collection privacy | Pass for observed host UI: only participation progress appeared after the first submission; the accepted Consequence was hidden. Hidden-media endpoint and cross-player ownership attacks were not tested. |
| Real generation transition | Pass: the final submission opened generation automatically, with no fixture banner or fixture substitution. Both screens showed “Preparing your story” and zero saved clips. |
| Failure handling | Pass: both screens reached a bounded failure result with “Generation timed out. Try another round after cleanup.” No contributions were disclosed and no replay was offered. |
| Refresh | Pass: host reload retained its identity and the same failure result; another-round control was available after cleanup. No new round was started. |
| Browser console | No warning/error entries returned by the host tab's console inspection after reload. |

## Actual provider evidence

Sanitized machine record: [2026-09-12-ww-cat-01-app.json](evidence/2026-09-12-ww-cat-01-app.json).

| Measurement | Observed |
| --- | --- |
| Authentication | Token HTTP 200 |
| Session startup/settings | 8.004 seconds |
| Place enqueue acknowledgment | 0.196 seconds |
| Place generation completion | 4.449 seconds after enqueue began |
| Advertised frames | 158 at enqueue and completion |
| Received frames | 157; all lacked optional frame identifiers/timestamps |
| Local buffer peak | 4 of 32 frames, 16,515,072 bytes; no overflow or capture error |
| Start event to first frame | 0.243 seconds |
| Start event to finish event | 6.502 seconds |
| Finish event to last frame | 0.091 seconds |
| Frame delivery span | 6.350 seconds |
| First-step deadline | 30.009 seconds |
| Cleanup | 0.635 seconds |
| Total attempt, including cleanup | 38.648 seconds |
| Independent closure | HTTP 200, `CLOSED` |
| Constraints requested | One session, maximum 180 seconds |
| Billable duration / actual cost | Unmeasured; total elapsed time is not a billing measurement. |

## Visual criteria and remaining work

| Reveal | Expected | Actual observation / media timestamp |
| --- | --- | --- |
| Place | Established enchanted forest | Not visually verified; generated clip failed capture, no saved media. |
| Character | Crowned fox appears in the forest | Not generated. |
| Action | Same crowned fox dances ballet | Not generated. |
| Consequence | Glowing snow falls around the same dancing fox | Not generated. |

Earlier detail retention, transition stalls/cuts, and replay after closure remain untested because the app saved zero clips. The incomplete temporary MP4 was removed by the existing capture cleanup. Raw private evidence remains under `.local/word-example-2026-09-12/word-by-word/live-evidence/`; the tracked record excludes session identity, credentials, and cookies.

This reproduces the capture shortfall seen in the historical third trial, now one frame short instead of two. The local queue did not overflow. Missing frame metadata prevents identifying where the frame was lost; the result does not establish whether the cause is transport, SDK delivery, event ordering, or provider frame accounting. Investigate capture boundaries or a reliable export path before another paid run. Keep the full-frame validation intact. Align the live category example hints with the standard scenarios separately.

Application source was not changed. This is one failed live scenario, not the specification's three-run acceptance campaign.
