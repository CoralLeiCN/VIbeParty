# Prompt Royale live round — 16 September 2026

A complete three-player live round passed in **86.4 seconds**, using the running
application at `http://localhost:8000` and real `reactor/fast-h3` generation.
Room **8121** produced three playable clips, accepted all three votes and declared
Alex the winner with two votes. Bailey received one vote and Casey received none.
No capture retried, and all three Reactor sessions were independently confirmed
closed. No application change or restart was needed for this rehearsal.

## Flow verified

The existing `completion.spec.ts` ran with `ROYALE_LIVE_E2E=1`, one worker and no
test retries. It created three independent Chromium contexts: two desktop viewports
and one 390 × 844 phone viewport. The host also participated as a player.

1. Created a three-player party, selected real generation and joined both guests.
2. Selected “The worst possible first day at a new job.” and started the round.
3. Set the host document's visibility properties to hidden for 35 seconds. More than
   20 successful room polls occurred during that interval, and the submission phase
   remained available. The test exercises the visibility branch; it does not simulate
   an operating system suspending a physical device.
4. Submitted distinct penguin, robot and dragon scenes through the player forms.
5. Reached screening with three eligible videos and no unscored result. Every client
   could start the arena. Host playback measurements confirmed all three videos
   completed a play and repeated, with no media errors.
6. Opened voting and submitted three votes through the three clients. Every client
   reached the winner screen. The server snapshot reported `scored: true`, one winner
   and three votes in total.
7. Selected Play again. The lobby kept its join URL and cleared the selected topic.
   This verified rematch setup; it did not start a second generation round.
8. Ended the room. The host returned to Create party, and resolving the retired join
   code returned HTTP 404. No browser page errors were reported.

The arena, desktop results and phone results screenshots were visually inspected.
They showed the three generated scenes, stable clip positions, the winner and the
expected vote totals. The fourth tile correctly showed “No entry.”

## Capture and media

All three attempts used the current shorter-clip adapter. Each requested six seconds;
FastH3 accepted 6.583 seconds. Each local output contained 120 received images saved
once at 24 fps, producing a five-second clip with no inserted image holds.

| Attempt | Received callbacks | Captured frames | Output | Lifecycle | Confirmed closed |
| --- | ---: | ---: | ---: | ---: | --- |
| `329061e8b0df4c368f330b77eb0f5f5a` | 151 | 120 | 5 s | 20.362 s | Yes |
| `39a6be574cb34028b37583b65c1fa9b0` | 152 | 120 | 5 s | 23.537 s | Yes |
| `2039bf9d5ec847118fac3d97969ab7e4` | 154 | 120 | 5 s | 18.940 s | Yes |

All capture stages ended at `complete`, with no recorded application queue overflow.
The frame cap is intentional: these saved game clips contain the first five seconds,
not the complete 6.583-second generated clip. Received callbacks are not verified
source-frame identities.

Independent FFprobe inspection of the three saved MP4s confirmed H.264, yuv420p,
1280 × 768, 24 fps, 120 frames, exactly five seconds and no audio stream. File sizes
were 2,272,748, 1,754,382 and 2,057,645 bytes in arena order.

This trial used the game's existing default `set_flush_on_clip_end: true`, with no
post-finish capture grace. The separately tested disabled-flush behavior has been
[documented](../../games/prompt-royale/fast-h3-flush-behavior.md), but has not been
implemented in the game. The success here therefore verifies the current game path.

## Cleanup and limits

These owned sessions were independently confirmed closed:

- `0dd86b08-791f-44cf-b6ec-5c7ea2c2cd28`
- `7acb210e-4c54-4e02-827d-481e7c7a500a`
- `f42252a1-8ca1-417f-bdf8-fa0386e2a7ec`

After the test, `/health` returned HTTP 200 with `status: ok`, `/api/session` reported
no active party, and the unresolved-provider marker was absent. The normal service
remained running and ready for another party.

This is one successful three-player Prompt Royale live round. It does not establish
four-player capacity, physical-phone playback, behavior during severe packet loss,
or live acceptance of Word by Word and Reverse Prompt. The synthetic sparse-frame
and mixed-duration checks are recorded in the
[shorter-clip verification](shorter-clips-2026-09-16.md).

## Retained evidence

Paths are relative to this checkout:

- `.local/prompt-royale-complete-live-2026-09-16/browser.log`: browser test result.
- `.local/prompt-royale-complete-live-2026-09-16/report/index.html`: Playwright report.
- `.local/prompt-royale-complete-live-2026-09-16/summary.json`: compact outcome and capture evidence.
- `.local/prompt-royale-complete-live-2026-09-16/media-validation.json`: FFprobe results.
- `.local/prompt-royale-complete-live-2026-09-16/assets/`: three MP4s, arena/results screenshots and result snapshot.
- `.local/exports/prompt-royale-live-2026-09-16/`: labeled copies of the videos for review.
- `.local/vibeparty/prompt-royale/diagnostics/`: individual attempt records named above.

The test started at 23:54:21 UTC on 15 September, or 00:54:21 on 16 September in
London. Its browser scenario took 86.4 seconds; runner time was about 87 seconds.
